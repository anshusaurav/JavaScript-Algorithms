/**
 * An arc of a circle with the same length as the radius of that circle 
 * subtends an angle of 1 radian. The circumference subtends an angle of 2π 
 * radians.
 * @param {*} radian 
 */
function radianToDegree(radian) {
    return radian * (180 / Math.PI);
}