/**
 * The length of an arc of a unit circle is numerically equal to the 
 * measurement in radians of the angle that it subtends; one radian is just 
 * under 57.3 degrees.
 * @param {*} degree 
 */
function degreeToRadian(degree) {
    return degree * (Math.PI / 180);
}